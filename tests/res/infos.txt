activity:
Checkstyle version: 9.3
Required removes:
    _remove_from_modules(root, "TreeWalker", "LineLength")
    _remove_from_modules(root, "TreeWalker", "JavadocMethod", "allowMissingJavadoc")
    _remove_from_modules(root, "TreeWalker", "JavadocMethod", "scope")
    _remove_from_modules(root, "TreeWalker", "JavadocMethod", "allowMissingThrowsTags")
    _remove_from_modules(
      root, "TreeWalker", "JavadocMethod", "allowThrowsTagsForSubclasses"
    )

alluxio:
new regex
Timeout

android-open-project-demo:
No pom.xml

api:
No checkstyle in pom.xml

areaShop:
old regex
Checkstyle version: 8.29
Required removes:
  _remove_from_modules(
      root, "TreeWalker", "JavadocMethod", "allowThrowsTagsForSubclasses"
  )
  _remove_from_modules(
      root, "TreeWalker", "JavadocMethod", "minLineCount"
  )

asqatasun
Version not found (8.18)
checkstyle.xml not found (on server?)

athenz:
new regex

aws-saas-boost:
works

checkstyle:
No checkstyle in pom.xml

coherence-spring:
Weired checkstyle file

onventional-commits-plugin:
No checkstyle file

cron-utils:
works

cucumber-jvm:
Exception in cucumber-jvm\cucumber-archetype\src\main\resources\archetype-resources\src\test\java\RunCucumberTest.java

DDF:
Requires: 2.6	-> 5.0

debezium:
Checkstyle error

DiskLruCache:
Requires: 2.10

dremio-oss:
Checkstyle error

druid:
worked

enterprise-spring-best-practices:
Requires: 2.9.1-> 5.5

flink-cdc-connectors:
works

framework:
works, but no output files

generator:
Not supported checkstyle version

geotoolkit:
No valid checkstyle file.

geotools:
Checkstyle exception geotools\docs\user\tutorial\quickstart\artifacts\module-info.java

gephi:
works

google-http-java-client:
works

graphhopper:
works

hadoop:
works. Contains multiple subprojects. For better results add logic to distinguish those
